<span>
#if ($user.isAuth() == true)##Пользователь залогинен
    $page.getLang().get("userArea.hello"), $user.getName()!

    <form action="" method="POST">
        <input type="hidden" name="act" value="logout" />
        <input type="submit" value="Выйти"/>
    </form>

#else
    <span onclick="$('#loginForm').show();">$page.getLang().get("userArea.enter")</span>

    <div id="loginForm" style="display: none;">
        <form action="" method="POST">
            <input type="text" name="login" />
            <input type="password" name="password"/>
            <input type="submit" value="$page.getLang().get("userArea.enter")"/>
            <input type="hidden" name="act" value="login"/>
            <button type="button" onclick="$('#loginForm').hide();" >$page.getLang().get("all.cancel")</button>

        </form>
    </div>
#end

    <a href="$page.getContextPath()/ru/">ru</a> <a href="$page.getContextPath()/en/">en</a>
</span>